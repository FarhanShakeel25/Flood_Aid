# API Base URL (no trailing slash). Must be set to avoid /undefined requests.
# When using Vite dev proxy (recommended for local to avoid CORS):
VITE_API_BASE=/api
VITE_PROXY_TARGET=https://localhost:7198

# For direct calls without proxy (ensure CORS is configured on the API):
# VITE_API_BASE=https://localhost:7198
# VITE_PROXY_TARGET=
# For ngrok or production:
# VITE_API_BASE=https://your-ngrok-url.ngrok-free.app
